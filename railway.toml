[build]
# Use Nixpacks and explicitly tell it how to build the backend.
builder = "NIXPACKS"
buildCommand = "cd backend && python3 -m pip install -r requirements.txt"

[start]
# Command Nixpacks should run to start the app.
cmd = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"


